{
  "name": "payment-project",
  "version": "1.0.0",
  "description": "Payment form with Vercel backend",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "razorpay": "^2.9.2"
  }
}
